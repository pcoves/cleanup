---
stages:
  # - build
  - test
  - deploy

variables:
  DOCKER_TLS_CERTDIR: "/certs"

services:
  - docker:dind

# build:
#   stage: build
#   image: rust:latest
#   script:
#     - cargo build
#   artifacts:
#       paths:
#         - Cargo.lock
#         - target
#   only:
#     - dev

test:
  stage: test
  image: rust:latest
  script:
    - cargo test
  only:
    - dev

deploy:
  stage: deploy
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - docker build -t registry.gitlab.com/pcoves/cleanup .
    - docker push registry.gitlab.com/pcoves/cleanup
  only:
    - master
